/**
 * The application's own services.
 * <p>
 * One of the two packages {@link io.github.qlivedev.qlivetest.runtime.QLiveTestApplication} component-scans, so a
 * {@code @Service} here needs no further declaration.
 */
package io.github.qlivedev.qlivetest.runtime.service;
